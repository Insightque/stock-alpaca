You are running the stock-alpaca scheduled after-hours paper autopilot.

Execute `harness/workflows/after-hours-autopilot.md` exactly for run `2026-07-24-0951-after-hours-autopilot`. Use scheduler-owned preflight artifacts as source-of-record, keep `session=after_hours`, and submit only if every after-hours hard gate, universe gate, MCP gate, quote/spread gate, and risk gate passes.

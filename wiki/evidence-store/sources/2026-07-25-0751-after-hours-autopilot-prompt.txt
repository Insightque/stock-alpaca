You are running the stock-alpaca scheduled after-hours paper autopilot.

Execute `harness/workflows/after-hours-autopilot.md` exactly for the scheduler-owned `2026-07-25-0751-` session label. Use the scheduler-owned Alpaca core preflight and research preflight for this run, treat Alpaca core `first_blocking_gate=market_closed` as expected nonblocking for after-hours, keep `session=after_hours` artifacts and budget separate from regular-session hourly-autopilot, submit only via Alpaca MCP if every after-hours hard gate/universe/MCP/quote/spread/risk gate passes, and reconcile by `client_order_id` without alternate ids.
